//
//  Grupo.swift
//  Tarefeitas
//
//  Created by Gisele on 04/11/22.
//  Copyright © 2022 Student. All rights reserved.
//

import Foundation

/*struct Grupo: Codable {
    
    let id: String?
    let nomeGrupo: String?
    let descricaoGrupo: String?
    

}
 
 var qtdTarefasConcluidas: Int = 0
 var listaTodasTarefas = []
 
 for (i) {
    for (j) {
        listaTodasTarefas.append(membros[i].tarefas[j])
        if (membros[i].tarefas[j].concluida == "true") {
            qtdTarefasConcluidas = tarefasConcluidas + 1
        }
    }
 }
progressBar = qtdTarefasConcluidas/(listaTodasTarefas.count)
porcentagem = progressBar * 100
 
 na pag de tarefas individuais:
 membroselecionado vai ser enviado:
 
 var qtdTarefasConcluidas: Int = 0
 var qtdTarefas = tarefas.count

    for (i) {
        if (tarefas[j].concluida == "true") {
            qtdTarefasConcluidas = tarefasConcluidas + 1
        }
    }
 
 
 tela de usuario pra disfarçar cadastro/login: json inteiro
 
 tela de grupos: carrega grupos da pessoa escolhida (json.grupos)
 
 telda detalheGrupo : (json.grupos.grupo) (json.grupos.descricao)
 
 tela de sobre grupo: (json.grupos.descricao) (json.grupos.grupo)
 
 tela de todas tarefas : (json.grupos.grupo)
 
 tela de tarefas individuais : //na segue pra cá: se json.grupos.grupo.pessoa[i].id = idEscolhido (pessoa escolhida inicialmente) passe >>> (json.grupos.grupo.pessoa[i]) // como fazer isso? - busca cloundant?
 
 
class gruposDAO {
    
    static func getList() -> [Grupo]{
        return[
            Grupo(id: "1", nomeGrupo: "Hackatruck", descricaoGrupo: "Data de entrega: 11/11. Não se esqueçam!"),
            Grupo(id: "2", nomeGrupo: "LP2", descricaoGrupo: "Não sei o que colocar aqui gente")
            
        ]
    }
}*/
